# Import the parent file (as a module) and see the difference in print statements.
import parent

# Check all the variables provided from the module import
# Namely look for the '__name__' variable
print(locals())
